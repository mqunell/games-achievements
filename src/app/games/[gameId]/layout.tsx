/*
TODO: Add dynamic metadata to replace this Head tag

<Head>
	<title>{`${gameCard.name} Achievements`}</title>
	<meta name="description" content={`${gameCard.name} achievements`} />
</Head>
*/

const GameAchievementsLayout = ({ children }: { children: React.ReactNode }) => {
	return children;
};

export default GameAchievementsLayout;
